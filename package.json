{
  "name": "hlidac-shopu",
  "version": "1.0.0",
  "description": "Hlídač Shopů",
  "private": true,
  "scripts": {
    "build": "npx webpack --mode=production --progress",
    "build:dev": "npx webpack --mode=development --progress",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "webpack": "^4.26.1",
    "webpack-cli": "^3.1.2"
  },
  "dependencies": {
    "chart.js": "^2.7.3",
    "dateformat": "^3.0.3"
  }
}
