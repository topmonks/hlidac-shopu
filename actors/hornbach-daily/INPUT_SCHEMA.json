{
    "title": "Input schema for the apify_project actor.",
    "type": "object",
    "schemaVersion": 1,
    "properties": {
        "country": {
            "title": "Country",
            "type": "string",
            "description": "...",
          "editor": "select",
          "enum": ["cz","sk"],
          "enumTitles": ["Česko","Slovensko"]
        }
    },
    "required": []
}
