{
  "title": "AAAAuto offer prices scraper",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "maxConcurrency": {
      "title": "Max concurrency",
      "type": "integer",
      "description": "How many scraping requests can run in parallel",
      "editor": "number",
      "minimum": 1,
      "maximum": 1000,
      "default": 1
    }
  },
  "required": ["maxConcurrency"]
}

